/**
 * Main content column for the dashboard overview and design-token category pages.
 * Keeps width and horizontal padding aligned with the overview (`/dashboard`).
 */
export const dashboardMainContentClassName =
  "mx-auto min-w-0 max-w-full w-[min(1000px,60%)] px-6 py-8 sm:px-10 sm:py-10";
